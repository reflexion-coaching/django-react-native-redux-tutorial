import { View } from 'react-native';
import CreateModal from '../modal/CreateModal';

const BookPost = () => {

    return (
        <View style={{ flex: 1 }}>
            <CreateModal />
        </View>
    )
}

export default BookPost;